# /plan

Create a concise plan before coding.

Output:
1) Steps
2) Files to create/modify (paths relative to repo root)
3) Risks/impacts (routes/auth/contracts/data)
4) Verification steps (`dotnet build`, manual testing)

Always check the reference feature at `src/BRF.Api/Features/Beers/GetAllBeers/` before planning.
Do not write code yet.